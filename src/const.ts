export const NAME = 'FSpyCamera';
export const VERSION = '1.0.0';

export const ErrorTexts: { [key: string]: string } = {
  loadError: "Please put fSpy's json path or parsed json in the first argument",
};
